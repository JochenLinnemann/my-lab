apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: server-claim2
  name: server-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1024Mi
